# RepBend (Llama-3.2-3B-Instruct) — 격리 런타임(Colab 별도 세션) 의존성.
# 우리 학습 코드는 trl 의 SFTTrainer 가 아니라 plain torch loop (단일 GPU,
# manual gradient accum) — accelerate / deepspeed 없이도 돌게 했음.
# peft 는 LoRA wrapping 에 필수 (저자 train.py 도 peft).
torch>=2.0.0
transformers>=4.40.0,<5.0
peft>=0.10.0
datasets>=2.18.0
safetensors
numpy>=1.26.0
accelerate>=0.29.0
huggingface_hub
tqdm
